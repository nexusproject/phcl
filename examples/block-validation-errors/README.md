# Block Validation Errors Example

This example intentionally fails during `phcl build`. It shows validation
errors for mappings that are valid JSON/YAML objects, but are not valid PHCL
block-shaped data.

Build it with PHCL:

```sh
phcl build examples/block-validation-errors
```
